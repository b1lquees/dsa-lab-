BILQUEES ASHFAQ JUMANI 
559015
BSCS 15 - E

task1.cpp – Builds a 5-element array, overwrites index 3 with 7, and prints the result with a for loop
task2.cpp – Reads 5 integers into an array in one loop, then sums them in a second loop and prints the total 
task3.cpp – Creates two Student objects, sets their fields directly, and uses a display method (which just echoes back whatever args are passed) to print them, showing that changing s1.marks doesn't affect s2.
task4.cpp – Reads 8 integers and tracks the first-occurring largest/smallest values and their indices in a single pass.
task6.cpp – Reverses a 6-element array in place via swapping from both ends toward the middle, then displays it.
task7.cpp – Removes duplicates in-place by scanning ahead and copying each not-yet-seen value into a "front region," tracked by count 